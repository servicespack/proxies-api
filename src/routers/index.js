import { docs } from './docs.router.js'
import { metrics } from './metrics.router.js'
import { proxies } from './proxies.router.js'

export class Routers {
  static docs = docs
  static metrics = metrics
  static proxies = proxies
}
